# warp to start location if holding start when opening the map screen.
02//checkWarpToStart: |
    ld a,(wKeysPressed)
    and BTN_B | BTN_A
    cp BTN_B | BTN_A
    jr nz,@done
    
    ld a,option.warpingGroup
    set 7,a
    ld (wWarpDestGroup),a
    ld a,option.warpingRoom        ; Warping room ID
    ld (wWarpDestRoom),a
    ld a,option.warpingPos         ; Position in warping room
    ld (wWarpDestPos),a
    ld a,option.warpingDestTransittion
    ld (wWarpTransition),a
    ld a,option.warpingSrcTransittion
    ld (wWarpTransition2),a
    ld a,$ff
    ld (wDisabledObjects),a
    
    ld a,SND_TELEPORT
    call playSound
    
    ld a,$03
    call setMusicVolume
    call clearStaticObjects
    
    ld a,$d0
    ld (wLinkObjectIndex),a
    
    ld a,$03
    ld (wMenuLoadState),a
    pop af      ; pop return addr from stack
    ret
    
    @done:
    jp $5068    ; @openMenu
# Replace the unique call to @openMenu by the above extension checking for warp to start
02/505e/: call checkWarpToStart
